import argparse
from auth import User
from type import Type
from core import Record

class Halo:

    def __init__(self):
        open('haloLog.csv', )


if __name__ == '__main__':
    while 1:
        try:
            cmd = input(">> ")
        except:
            break